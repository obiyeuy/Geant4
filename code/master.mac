# 设置线程数为虚拟机的满载 8 线程
/run/numberOfThreads 8

# 核心初始化（只执行这一次，节省大量时间）
/run/initialize

/control/verbose 0
/run/verbose 1
/event/verbose 0
/tracking/verbose 0
/run/printProgress 1000000

# 开启内置循环
# 参数含义：[单行宏文件名] [循环变量名] [初始值] [终止值] [步长]
/control/loop scan_row.mac iRow -28 28 0.7